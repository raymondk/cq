2026-04-29: Completed Slice 1 (issue #2) — end-to-end skeleton. Cargo project initialized with candid/candid_parser/clap/anyhow/hex. Six-module structure: cli, input (autodetect text/hex/bin), query (identity), schema (stub), output (candid/hex/bin), main wire. `echo '(record { foo = 1 : nat })' | cq` works; exit 1 on parse failure. 14 integration tests passing (identity round-trips for record/variant/opt/vec/bool/int/nat/text/principal/blob/null, error handling, cq|cq lossless, hex round-trip). Issue #2 closed.
2026-04-29: Completed Slice 2 (issue #3) — multi-format I/O with streaming. Added bin_frame.rs: a DIDL binary frame-size calculator that uses LEB128/SLEB128 parsing to implement parse-and-advance without magic scanning (handles all primitive types, opt, vec, record, variant, func, service, future types). Refactored input.rs into a streaming iterator (CandidStream) with three backends — TextStream (paren-depth tracker splits (…) groups), HexStream (line-by-line BufRead), BinStream (parse-and-advance via frame_size). Added "text" as alias for "candid" in both input/output format strings. Updated main.rs to use streaming. 32 integration tests passing (all 9 3×3 round-trip pairs, multi-value text/hex/bin streaming, malformed-input error tests, format-alias tests). Issue #3 closed.
2026-04-29: Completed Slice 3 (issue #4) — field access and pipe. Rewrote query.rs with a recursive-descent parser and evaluator. Supports: identity `.`, field access `.foo` (strict — errors on unknown field with available-fields list in stderr), chained access `.foo.bar.baz` (desugared to nested Pipe nodes), pipe `expr1 | expr2` (left-to-right composition). Hash-based lookup also works for binary-decoded records via candid::idl_hash. 10 new integration tests; 42 total, all passing. Issue #4 closed.
2026-04-29: Completed Slice 4 (issue #7) — vec ops with multi-value streams. Extended query.rs parser and evaluator with three new AST nodes: Index(usize) for `.[i]`, Slice(Option<usize>, Option<usize>) for `.[i:j]`, and Iter for `.[]`. Parser extended in parse_chain to detect `.[` and dispatch to parse_bracket; chained bracket expressions (e.g. `.[0].name`, `.items.[1]`) work via the same Pipe desugaring. Evaluator: Index returns the i-th element (error on out-of-bounds), Slice returns a sub-vec value (clamps end to len), Iter returns one IDLArgs per element (empty vec produces no output, exit 0). Multi-value emission already handled by existing main loop. 11 new integration tests; 53 total, all passing. Issue #7 closed.
2026-04-29: Completed Slice 5 (issue #8) — variant arm access. Extended Expr::Field with an optional bool flag. `.Tag` on a variant extracts the arm payload when the tag matches; errors with "tag mismatch: active tag is X" on mismatch. `.Tag?` (optional form) produces empty output on mismatch, composes with chained traversal (e.g. `.kind.Transfer?.amount`). Null-payload arms (e.g. `variant { Pending }`) return `(null : null)`. Record field access behavior unchanged. Extracted chain_tail helper for cleaner parse_chain structure. 9 new integration tests; 62 total, all passing. Issue #8 closed.
2026-04-29: Completed Slice 6 (issue #9) — opt handling. Extended Expr::Field bool flag to OptMode enum (Normal/Optional/Assert). `.field` on opt-field returns opt-wrapped value (lossless). `.field?` unwraps opt-Some to inner value; produces no output for opt-None (IDLValue::None); composes in chains (`.address?.city` skips entire chain when None). `.field!` unwraps opt-Some; errors with clear message on opt-None. Added grammar levels: parse_atom (handles `some(expr)` and `none` atoms before dotchain), parse_alt (`expr // fallback`). `//` evaluates left, unwraps if opt-Some, uses fallback if empty/None. `some(expr)` wraps result in IDLValue::Opt. `none` emits IDLValue::None. Added `.?` as standalone opt-unwrap on the top-level value. 12 new integration tests; 74 total, all passing. Issue #9 closed.
2026-04-29: Completed Slice 7 (issue #10) — construction syntax. Added TypeAscription enum and 7 new Expr variants: MakeRecord, MakeVec, MakeVariant, MakePrincipal, MakeBlob, BlobHex, Ascribe. Grammar: inserted parse_ascribe level (between pipe and alt) for postfix `: TypeName` coercion; parse_atom extended for `{field: expr}` record construction, `[expr, ...]` vec construction, `variant { Tag = expr }` / `variant { Tag }` variant construction, `principal "text"` (validated by candid::Principal::from_text), `blob "..."` (candid escape string), `blob_hex(expr)` (hex-decode text value). Type ascription supports nat8/nat16/nat32/nat64/int8/int16/int32/int64/nat/int/float32/float64; range-check errors on overflow. parse_quoted_bytes helper handles \n \t \r \" \\ \XX hex escapes. 17 new integration tests; 91 total, all passing. Issue #10 closed.
2026-04-29: Completed Slice 8 (issue #11) — arithmetic, comparison, select, booleans. Added num-bigint = "0.4" dependency for bigint-during-evaluation semantics. Added ArithOp/CmpOp/BoolOp enums and 6 new Expr variants: Literal, BinArith, BinCmp, BinBool, Not, Select. Extended grammar with 6 new levels (parse_or/and/not/cmp/add/mul) between parse_alt and parse_atom; parse_atom extended for non-negative integer literals, true/false boolean literals, and select(predicate). Evaluator: Num enum (Unsigned(BigUint)/Signed(BigInt)/Float(f64)) for type-kind-tracking arithmetic; nat - nat negative result widens to signed int; sized-integer overflow errors only at ascription boundary; float/int mixing produces a clear error; comparison works across signed/unsigned via BigInt promotion; select passes or filters values based on boolean predicate; and/or/not compose booleans. 29 new integration tests; 120 total, all passing. Issue #11 closed.
2026-04-29: Completed Slice 9 (issue #15) — variant match and tag introspection. Added MatchArm enum (Tag(String)/Default) and 2 new Expr variants: Match(arms) and Tag(inner). parse_atom extended for match { Tag1 = body1; Tag2 = body2; _ = default } and tag(expr); parse_match_arms helper parses semicolon-separated arms with = separating name and body; _ acts as catch-all default. Evaluator: Match dispatches to matching arm's body with arm payload bound as implicit input (. inside body sees the payload, null for null-payload arms); no match and no default produces clear error; Tag evaluates inner expr, expects a variant, and returns the tag name as IDLValue::Text. Also fixed two missing features discovered during testing: string literals "..." in parse_atom (→ IDLValue::Text), and text equality/ordering in BinCmp evaluator (enables select(tag(.) == "Transfer") patterns). 11 new integration tests; 131 total, all passing. Issue #15 closed.
2026-04-29: Completed Slice 10 (issue #12) — control structures: if/elif/else, as $x variable binding, string interpolation. Added StrPart enum (Lit/Expr) and 4 new Expr variants: If(branches, else_branch), VarBind(name, bound, body), VarRef(name), StrInterp(parts). Grammar: parse_pipe extended to detect `as $x |` before plain `|` — binds expr's result to $x in a new env scope, body evaluates with original input; parse_atom extended for `if cond then body [elif cond then body]* [else body] end` blocks and `$x` variable references; string literal parsing promoted to parse_interp_string which handles `\(expr)` interpolation segments alongside existing escapes. Evaluator: threaded HashMap<String, IDLValue> env parameter through eval_expr and eval_single_bool; If evaluates conditions in order and returns first matching branch (empty output if no match and no else); VarBind evaluates bound expr, extends env with $x, evaluates body with original input; VarRef looks up $x in env; StrInterp concatenates literal parts and expr parts (non-text values converted via idl_to_text which gives plain numbers not "42 : nat"). 15 new integration tests; 146 total, all passing. Issue #12 closed.
2026-04-29: Completed Slice 11 (issue #13) — generic + conversion builtins. Added require_single_val and type_tag helpers. Added 19 new Expr variants: Length, Keys, Values, TypeOf, Has, Contains, Map, ToText, ToInt, ToFloat, ToPrincipal, ToHex, FromHex, ToUtf8, FromUtf8, IsSome, IsNone (and updated BinCmp for principal ==/</>). Grammar: parse_atom extended with keyword builtins (length, keys, values, type, to_text, to_int, to_float, to_principal, to_hex, from_hex, to_utf8, from_utf8, is_some, is_none) and function-style builtins (has(expr), contains(expr), map(filter)). Evaluator: length works on vec/text/blob; keys/values sort record fields by name; type returns candid type tag string; has checks field presence by name; contains checks text substring or vec membership using IDLValue structural equality; map applies filter to each vec element and collects flat results; all conversion builtins (to_text/to_int/to_float/to_principal/to_hex/from_hex/to_utf8/from_utf8) implemented with clear errors; is_some/is_none predicates on opt values; principal ==/</>/<= etc. use canonical text representation ordering. 33 new integration tests; 179 total, all passing. Issue #13 closed.
2026-04-29: Completed Slice 12 (issue #16) — analysis builtins: sort, sort_by, group_by, unique. Added cmp_idl_values helper (natural ordering for bool/text/principal/numerics; mixed-type errors). Added 4 new Expr variants: Sort, SortBy(Box<Expr>), GroupBy(Box<Expr>), Unique. Grammar: parse_atom extended with sort_by( / sort / group_by( / unique (sort_by before sort to avoid prefix collision; group_by is function-style). Evaluator: Sort sorts a vec by natural ordering of its elements using cmp_idl_values; SortBy computes a key per element via the filter, then sorts by those keys; GroupBy groups elements by filter value preserving first-occurrence order of groups; Unique deduplicates using IDLValue PartialEq preserving first-occurrence order. Empty vecs produce empty vecs for all four. 15 new integration tests; 194 total, all passing. Issue #16 closed.
2026-04-29: Completed Slice 13 (issue #5) — schema loader: --did flag. Changed cli.rs did field from Option<PathBuf> to Vec<PathBuf> to support multiple --did flags. Rewrote schema.rs: load() accepts &[&Path] and merges multiple .did files into one TypeEnv via successive candid_parser::check_prog calls; build_hash_map() recursively walks all types in the TypeEnv (Record/Variant/Opt/Vec/Func/Service/Var with cycle guard) collecting Named labels and inserting idl_hash(name) → name into a HashMap<u32, String>. Updated output.rs: emit() accepts a &HashMap<u32, String>; when non-empty, resolve_args/resolve_value/resolve_label walk the IDLArgs tree and replace Label::Id(hash) with Label::Named(name) for known hashes before rendering. Updated main.rs to build did_refs and thread schema.hash_to_name into emit. Created tests/fixtures/schema1.did (MyRecord/Result types), schema2.did (Extra type), bad.did (malformed). 7 new integration tests; 201 total, all passing. Issue #5 closed.
2026-04-29: Completed Slice 14 (issue #14) — idl_hash auto-resolution + .[hash] numeric fallback. .field_name on hash-keyed records already worked via label_matches (which computes idl_hash(name) and checks against Label::Id). Extended extract_index in query.rs to dispatch on record vs vec: for Record, treats the index n as a u32 hash (with bounds check for >u32::MAX) and iterates fields matching either Label::Id(n)/Label::Unnamed(n) == hash or Label::Named(name) where idl_hash(name) == hash — so .[n] works whether the field was decoded from binary (hash label) or text (named label); gives "no field with hash N" on miss. Vec behavior unchanged. 7 new integration tests; 208 total, all passing. Issue #14 closed.
